import MockEmptyRepository from "./MockEmptyRepository";

/**
 * You must NEVER RENAME the properties of this class.
 * Prop names are used as type for a DB model.
 *
 * @see {Forum}
 */
export class InstalledEngines {
    readonly mockEmpty = new MockEmptyRepository();
}
